define([
  'underscore',
  'jquery',
  'backbone'
], function(_, $, Backbone) {
  var orderModel = Backbone.Model.extend({
    

    initialize: function() {
      
    }

  });

  return orderModel;

});
